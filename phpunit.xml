<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         beStrictAboutOutputDuringTests="false"
         failOnWarning="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnRisky="true"
         displayDetailsOnTestsThatTriggerWarnings="true"
         displayDetailsOnTestsThatTriggerErrors="true"
         displayDetailsOnTestsThatTriggerNotices="true"
         displayDetailsOnIncompleteTests="true"
         displayDetailsOnSkippedTests="true">
    <testsuites>
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>includes</directory>
        </include>
        <exclude>
            <directory>includes/PdfParser</directory>
            <directory>includes/smsgateapi_sluzba_cz</directory>
            <file>includes/net/routeros_api.class.php</file>
            <file>includes/net/SSH2.php</file>
            <file>includes/net/email/MimeDecode.php</file>
        </exclude>
    </source>
    <!--
      No coverage report block here on purpose: a report block makes every
      run attempt coverage collection, which warns (and returns exit 1) when
      no driver is installed. Coverage is opt-in via `composer test:coverage`,
      which requests it with CLI coverage flags and honours the source scope.
    -->
    <php>
        <env name="APP_ENV" value="testing"/>
    </php>
</phpunit>
